{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for RedCode IDE",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
